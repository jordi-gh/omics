## Variables globals comunes, cal comitejar aquest fitxer

# Connexió a bd sqlite: metadata
assign("db_meta", NULL, envir = .GlobalEnv)
# Connexió a bd SQLite: GEOMetadb
assign("db_geo", NULL, envir = .GlobalEnv)